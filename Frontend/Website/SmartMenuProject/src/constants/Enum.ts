export enum CurrentForm {
  BRAND,
  BRANCH,
}

export enum UserRole {
  Admin = 1,
  BrandManager = 2,
  BranchManager = 3,
}

export enum PaymentStatus {
  Succeed = 1,
  Failed = 2,
  Pending = 3,
  Cancelled = 4,
}
